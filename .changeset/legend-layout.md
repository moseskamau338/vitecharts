---
'@vitecharts/core': patch
---

Reserve space for the legend when sizing the plot, so the SVG and a bottom/top
(or left/right) legend fit the container together instead of the legend
overflowing past it.
